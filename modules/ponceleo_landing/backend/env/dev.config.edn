{
 ;; Public dev environment variables
 ;; Overrides config.edn
 ;; Used by the `yogthos/config lib
 ;; (config.core/env :keyword) gets an env variable
 ;; (config.core/reload-env) reload env variables for current REPL

 :dev true
 ;; I'm using maildev as decribed here https://www.grafikart.fr/tutoriels/maildev-tester-emails-595
 ;; In local environment
 :mail-server
 {:default-sender "le.site@ponceleo.com"
  :host "localhost"
  :port 1025
  :ssl false
  :user nil
  :pass nil}}